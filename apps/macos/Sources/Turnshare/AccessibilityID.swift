/// Centralised accessibility identifiers and labels for UI-test and VoiceOver support.
enum AccessibilityID {
    // MARK: - Settings Gear Button
    static let settingsButtonLabel = "Settings"

    // MARK: - Toggles (identifiers)
    static let confirmBeforePublishingToggle = "confirmBeforePublishing"
    static let launchAtLoginToggle = "launchAtLogin"

    // MARK: - Toggles (labels)
    static let confirmBeforePublishingLabel = "Confirm before publishing"
    static let launchAtLoginLabel = "Launch at login"
}
